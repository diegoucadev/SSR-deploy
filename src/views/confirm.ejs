<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Confirmacion</title>
    <link rel="stylesheet" href="/css/output.css" />
  </head>
  <body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="bg-white rounded-lg shadow-lg p-8 max-w-md w-full">
      <h1 class="text-3xl font-semibold text-center text-gray-800 mb-6">
        Confirmation de procesamiento de datos
      </h1>

      <div class="bg-gray-50 p-6 rounded-md shadow-md mb-6">
        <h2 class="text-2xl font-bold mb-4 text-gray-700">Tus datos:</h2>

        <div class="space-y-4">
          <p class="text-lg">
            <span class="font-semibold">Nombre:</span>
            <span class="text-gray-600"><%= name || 'N/A' %></span>
          </p>
          <p class="text-lg">
            <span class="font-semibold">Edad:</span>
            <span class="text-gray-600"><%= age || 'N/A' %></span>
          </p>
          <p class="text-lg">
            <span class="font-semibold">Email:</span>
            <span class="text-gray-600"><%= email || 'N/A' %></span>
          </p>
        </div>
      </div>

      <div class="text-center">
        <a
          href="/form"
          class="inline-block bg-blue-500 text-white px-6 py-3 rounded-md hover:bg-blue-600 transition duration-200"
          >Regresar</a
        >
        <a
          href="/"
          class="inline-block ml-4 bg-gray-300 text-gray-800 px-6 py-3 rounded-md hover:bg-gray-400 transition duration-200"
          >Inicio</a
        >
      </div>
    </div>
  </body>
</html>
